# Optimization History Moved

The project-level optimization history now lives at:

```text
../OPTIMIZATION_HISTORY.md
```

This file used to track only `randomized_svd_baseline_v2`, but the experiment
log now covers shared `turboquant/` kernels, v2 single-node runs, and v3
multi-node compressed MPI collectives.
